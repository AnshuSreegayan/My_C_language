#include<conio.h>
#include<stdio.h>
int main()
{
	int x=30,y=40;
	if(x==y)
	printf("x is equal to y");
	elseif (x>y)
	printf("x is greater then y");
	elseif(x<y)
	printf("y is greater then x");

	return 0;
	
}

//Output is error because elseif is not any reserve word and compiler does not understand 

