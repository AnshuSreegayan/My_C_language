#include<stdio.h>
#include<conio.h>
int main()
{
	int i=10,j=20;
	if (i=5) && if (j=10)
		printf("\n have a nice day");
		
	return 0;
	
}

// output of the this program is Error because if and if is not valid 
