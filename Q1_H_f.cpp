#include<stdio.h>
#include<conio.h>

int main()
{
	int kk=65,ll;
	ll=(kk==65:printf("\n kk is equl to 65"):printf("\n kk is not equalto 65"));
	printf("%d",ll);
	
	return 0;
	  
}

//output of the program is error because in line no. 7 is  ? is missing
